"use client";
import { useEffect,useState } from "react"; import Guard from "@/components/Guard"; import Title from "@/components/Title"; import type { Produto } from "@/lib/types";
function Content(){const [tipo,setTipo]=useState('RETORNO'),[produtos,setProdutos]=useState<Produto[]>([]),[produtoId,setProdutoId]=useState(''),[pedidoId,setPedidoId]=useState(''),[quantidade,setQuantidade]=useState(1),[nome,setNome]=useState(''),[descricao,setDescricao]=useState(''),[preco,setPreco]=useState(''),[obs,setObs]=useState(''),[msg,setMsg]=useState('');function load(){fetch('/api/produtos').then(r=>r.json()).then(d=>setProdutos(d.produtos||[]))}useEffect(load,[]);async function salvar(e:React.FormEvent){e.preventDefault();const body=tipo==='NOVO_ITEM'?{tipo,nome,descricao,preco:Number(preco),quantidade}:{tipo,produtoId:Number(produtoId),pedidoId:tipo==='RETORNO'?Number(pedidoId):undefined,quantidade,observacao:obs};const res=await fetch('/api/estoque',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),d=await res.json();setMsg(res.ok?'Operação salva com sucesso.':d.error||'Falha ao salvar');if(res.ok){load();setQuantidade(1);setNome('');setDescricao('');setPreco('');setObs('')}}return <><Title title="Movimentação de estoque" sub="Registre retornos, reposições e novos itens sem perder o histórico."/><div style={{display:'flex',gap:9,marginBottom:18,flexWrap:'wrap'}}>{[['RETORNO','Retorno'],['REPOSICAO','Reposição'],['NOVO_ITEM','Novo item']].map(([v,l])=><button key={v} className={'btn '+(tipo===v?'btn-primary':'btn-soft')} onClick={()=>{setTipo(v);setMsg('')}}>{l}</button>)}</div><form className="card" onSubmit={salvar} style={{padding:24,maxWidth:760,display:'grid',gap:14}}>{tipo!=='NOVO_ITEM'?<><label>Item<select className="input" required value={produtoId} onChange={e=>setProdutoId(e.target.value)}><option value="">Selecione...</option>{produtos.map(p=><option key={p.id} value={p.id}>{p.nome} — estoque atual: {p.estoque}</option>)}</select></label>{tipo==='RETORNO'&&<label>Número do pedido<input className="input" required type="number" min="1" value={pedidoId} onChange={e=>setPedidoId(e.target.value)}/></label>}<label>Quantidade<input className="input" required type="number" min="1" value={quantidade} onChange={e=>setQuantidade(Math.max(1,Number(e.target.value)))}/></label><label>Observação<textarea className="input" rows={3} value={obs} onChange={e=>setObs(e.target.value)} placeholder={tipo==='RETORNO'?'Motivo do retorno...':'Informação opcional...'}/></label></>:<><label>Nome do item<input className="input" required value={nome} onChange={e=>setNome(e.target.value)}/></label><label>Descrição<input className="input" value={descricao} onChange={e=>setDescricao(e.target.value)}/></label><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:12}}><label>Preço<input className="input" required type="number" min="0" step="0.01" value={preco} onChange={e=>setPreco(e.target.value)}/></label><label>Estoque inicial<input className="input" required type="number" min="0" value={quantidade} onChange={e=>setQuantidade(Math.max(0,Number(e.target.value)))}/></label></div></>}{msg&&<div style={{padding:12,borderRadius:10,background:'#fff4eb',fontWeight:700}}>{msg}</div>}<button className="btn btn-primary">Salvar no banco</button></form></>};export default function Page(){return <Guard><Content/></Guard>}
